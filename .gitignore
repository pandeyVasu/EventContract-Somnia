node_modules/
dist/
.env
.env.*
# ...but the template of it is documentation, and is meant to be published.
!.env.example

# Local-only: planning notes, reference material, agent config. Never published.
context/
reference/
.claude/
.playwright-mcp/

# Local design notes and agent instructions. Never published.
farm-game-*.md
CLAUDE.md

*.pem
*.key
credentials*

# Spike run artifacts: wallet-specific, regenerated by the scripts.
spike/market.json
spike/placed.json
spike/combo.json

# Local review notes, kept out of the published repo like the other working docs.
Harvest-Call-Audit-Report.md
